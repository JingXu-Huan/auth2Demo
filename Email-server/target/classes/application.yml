server:
  port: 8013
  # 用户服务网关地址（用于API调用）
  user-service-url: http://localhost:9000
  # 前端地址（用于邮件链接）
  frontend-url: http://localhost:5173

spring:
  application:
    name: email-server
  cloud:
    nacos:
      discovery:
        server-addr: 211.101.236.212:8848  # Nacos 服务器地址
        namespace: public                # 命名空间
        group: DEFAULT_GROUP               # 分组
        username: nacos
        password: nacos
      config:
        import-check:
          enabled: false                   # 禁用配置导入检查
  # 邮件配置
  mail:
    host: smtp.qq.com
    port: 587
    username: yangjj1235@qq.com
    password: liunfvzmfjyodfde
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://134.175.227.4:5432/admin_db
    username: lantis-admin
    password: lantis_admin
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password:   # 生产环境必须设置密码
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
rocketmq:
  name-server: 211.101.236.212:9876
  consumer:
    group: email_consumer_group
    consume-thread-min: 5
    consume-thread-max: 20


# 服务间认证配置
service:
  auth:
    secret: your_32_character_secret_key_here_12345  # 必须至少32位

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.email.model
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      table-prefix: ""
