# 测试规范文档 (Testing Standards Document)

## 📋 文档信息

| 项目信息 | 详情 |
|---------|------|
| **项目名称** | AIO 企业级即时通讯系统 |
| **文档类型** | 测试规范文档 |
| **文档版本** | v1.0.0 |
| **创建日期** | 2025-11-12 |
| **测试团队** | QA测试团队 |

---

## 🎯 测试策略

### 测试目标
1. **功能完整性**: 验证所有功能需求的正确实现
2. **性能达标**: 确保系统性能满足指标要求
3. **安全可靠**: 验证系统安全性和稳定性
4. **用户体验**: 保证良好的用户交互体验
5. **兼容性**: 确保多平台兼容性

### 测试原则
- **测试左移**: 在开发早期介入测试
- **自动化优先**: 提高测试效率和覆盖率
- **风险驱动**: 重点测试高风险功能
- **持续集成**: 集成到CI/CD流程中

---

## 🧪 测试分层策略

### 测试金字塔
```
        ┌─────────────────┐
        │   E2E测试 (10%)  │  ← 端到端测试
        └─────────────────┘
      ┌───────────────────────┐
      │   集成测试 (20%)       │  ← 服务间集成测试
      └───────────────────────┘
    ┌─────────────────────────────┐
    │     单元测试 (70%)           │  ← 单元功能测试
    └─────────────────────────────┘
```

### 1. 单元测试 (Unit Testing)
**覆盖率要求**: ≥80%

**测试范围**:
- 业务逻辑层方法
- 数据访问层方法
- 工具类和帮助方法
- 异常处理逻辑

**测试框架**:
- **JUnit 5**: Java单元测试框架
- **Mockito**: Mock对象框架
- **TestContainers**: 集成测试容器

**示例测试用例**:
```java
@ExtendWith(MockitoExtension.class)
class UserServiceTest {
    
    @Mock
    private UserRepository userRepository;
    
    @InjectMocks
    private UserService userService;
    
    @Test
    @DisplayName("用户注册成功测试")
    void testRegisterUser_Success() {
        // Given
        RegisterRequest request = new RegisterRequest();
        request.setUsername("testuser");
        request.setPassword("password123");
        request.setEmail("test@example.com");
        
        User savedUser = new User();
        savedUser.setId(1L);
        savedUser.setUsername("testuser");
        
        when(userRepository.existsByUsername("testuser")).thenReturn(false);
        when(userRepository.save(any(User.class))).thenReturn(savedUser);
        
        // When
        UserResponse response = userService.register(request);
        
        // Then
        assertThat(response.getUserId()).isEqualTo(1L);
        assertThat(response.getUsername()).isEqualTo("testuser");
        verify(userRepository).save(any(User.class));
    }
    
    @Test
    @DisplayName("用户名已存在注册失败测试")
    void testRegisterUser_UsernameExists() {
        // Given
        RegisterRequest request = new RegisterRequest();
        request.setUsername("existuser");
        
        when(userRepository.existsByUsername("existuser")).thenReturn(true);
        
        // When & Then
        assertThatThrownBy(() -> userService.register(request))
            .isInstanceOf(BusinessException.class)
            .hasMessage("用户名已存在");
    }
}
```

### 2. 集成测试 (Integration Testing)
**覆盖率要求**: ≥70%

**测试范围**:
- 服务间接口调用
- 数据库操作集成
- 消息队列集成
- 缓存集成测试

**测试环境**:
- **Spring Boot Test**: 集成测试框架
- **TestContainers**: 容器化测试环境
- **WireMock**: 外部服务Mock

**示例测试用例**:
```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@Testcontainers
class MessageServiceIntegrationTest {
    
    @Container
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:13")
            .withDatabaseName("test_db")
            .withUsername("test")
            .withPassword("test");
    
    @Container
    static GenericContainer<?> redis = new GenericContainer<>("redis:6.2")
            .withExposedPorts(6379);
    
    @Autowired
    private TestRestTemplate restTemplate;
    
    @Test
    @DisplayName("发送消息集成测试")
    void testSendMessage_Integration() {
        // Given
        SendMessageRequest request = new SendMessageRequest();
        request.setReceiverId(2L);
        request.setContent("Hello World");
        request.setMessageType("text");
        
        // When
        ResponseEntity<ApiResponse> response = restTemplate.postForEntity(
            "/api/v1/messages/private", request, ApiResponse.class);
        
        // Then
        assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
        assertThat(response.getBody().getCode()).isEqualTo(200);
    }
}
```

### 3. 端到端测试 (E2E Testing)
**覆盖率要求**: 核心业务流程100%

**测试范围**:
- 用户注册登录流程
- 消息发送接收流程
- 文件上传下载流程
- 群组创建管理流程

**测试工具**:
- **Selenium**: Web自动化测试
- **Cypress**: 现代E2E测试框架
- **Playwright**: 跨浏览器测试

**示例测试用例**:
```javascript
// Cypress E2E测试示例
describe('用户登录流程', () => {
  beforeEach(() => {
    cy.visit('/login');
  });

  it('应该能够成功登录', () => {
    // 输入用户名和密码
    cy.get('[data-cy=username]').type('testuser');
    cy.get('[data-cy=password]').type('password123');
    
    // 点击登录按钮
    cy.get('[data-cy=login-btn]').click();
    
    // 验证登录成功
    cy.url().should('include', '/dashboard');
    cy.get('[data-cy=user-avatar]').should('be.visible');
  });

  it('应该显示错误信息当密码错误时', () => {
    cy.get('[data-cy=username]').type('testuser');
    cy.get('[data-cy=password]').type('wrongpassword');
    cy.get('[data-cy=login-btn]').click();
    
    cy.get('[data-cy=error-message]')
      .should('be.visible')
      .and('contain', '用户名或密码错误');
  });
});
```

---

## 🚀 性能测试

### 1. 负载测试
**测试目标**: 验证系统在预期负载下的性能表现

**测试指标**:
- **并发用户数**: 10,000
- **响应时间**: 95% < 200ms
- **吞吐量**: > 5,000 QPS
- **错误率**: < 0.1%

**测试工具**: JMeter, K6, Gatling

**测试场景**:
```javascript
// K6负载测试脚本示例
import http from 'k6/http';
import { check, sleep } from 'k6';

export let options = {
  stages: [
    { duration: '2m', target: 100 },   // 2分钟内增加到100用户
    { duration: '5m', target: 100 },   // 保持100用户5分钟
    { duration: '2m', target: 1000 },  // 2分钟内增加到1000用户
    { duration: '5m', target: 1000 },  // 保持1000用户5分钟
    { duration: '2m', target: 0 },     // 2分钟内降到0用户
  ],
};

export default function() {
  // 登录测试
  let loginResponse = http.post('http://localhost:9000/api/v1/users/login', {
    username: 'testuser',
    password: 'password123'
  });
  
  check(loginResponse, {
    '登录状态码为200': (r) => r.status === 200,
    '登录响应时间<200ms': (r) => r.timings.duration < 200,
  });
  
  let token = loginResponse.json('data.accessToken');
  
  // 发送消息测试
  let messageResponse = http.post('http://localhost:9000/api/v1/messages/private', {
    receiverId: 2,
    content: 'Load test message',
    messageType: 'text'
  }, {
    headers: { 'Authorization': `Bearer ${token}` }
  });
  
  check(messageResponse, {
    '消息发送状态码为200': (r) => r.status === 200,
    '消息发送响应时间<100ms': (r) => r.timings.duration < 100,
  });
  
  sleep(1);
}
```

### 2. 压力测试
**测试目标**: 找到系统的性能极限

**测试方法**:
- 逐步增加负载直到系统崩溃
- 记录系统的最大承载能力
- 分析性能瓶颈点

### 3. 稳定性测试
**测试目标**: 验证系统长时间运行的稳定性

**测试方法**:
- 持续运行24小时以上
- 监控内存泄漏
- 监控系统资源使用

---

## 🔒 安全测试

### 1. 认证授权测试
**测试内容**:
- JWT Token有效性验证
- 权限控制测试
- 会话管理测试

**测试用例**:
```java
@Test
@DisplayName("无效Token访问测试")
void testInvalidTokenAccess() {
    // Given
    String invalidToken = "invalid.jwt.token";
    
    // When
    ResponseEntity<String> response = restTemplate.exchange(
        "/api/v1/users/profile",
        HttpMethod.GET,
        new HttpEntity<>(createHeaders(invalidToken)),
        String.class
    );
    
    // Then
    assertThat(response.getStatusCode()).isEqualTo(HttpStatus.UNAUTHORIZED);
}

@Test
@DisplayName("权限不足访问测试")
void testInsufficientPermissionAccess() {
    // Given - 普通用户Token
    String userToken = generateUserToken();
    
    // When - 访问管理员接口
    ResponseEntity<String> response = restTemplate.exchange(
        "/api/v1/admin/users",
        HttpMethod.GET,
        new HttpEntity<>(createHeaders(userToken)),
        String.class
    );
    
    // Then
    assertThat(response.getStatusCode()).isEqualTo(HttpStatus.FORBIDDEN);
}
```

### 2. 输入验证测试
**测试内容**:
- SQL注入防护
- XSS攻击防护
- 参数校验测试

### 3. 数据安全测试
**测试内容**:
- 敏感数据加密
- 传输安全测试
- 数据泄露防护

---

## 🌐 兼容性测试

### 1. 浏览器兼容性
**测试范围**:
| 浏览器 | 版本 | 测试重点 |
|--------|------|---------|
| Chrome | 70+ | 完整功能测试 |
| Firefox | 65+ | 完整功能测试 |
| Safari | 12+ | WebSocket兼容性 |
| Edge | 79+ | 文件上传功能 |

### 2. 移动端兼容性
**测试设备**:
- iOS: iPhone 12, iPad Air
- Android: Samsung Galaxy S21, Huawei P40

### 3. 网络环境测试
**测试场景**:
- 4G网络环境
- WiFi网络环境
- 弱网络环境
- 网络中断恢复

---

## 📱 前端测试规范

### 1. 组件测试
**测试框架**: Vue Test Utils + Jest

**测试示例**:
```javascript
import { mount } from '@vue/test-utils';
import MessageInput from '@/components/MessageInput.vue';

describe('MessageInput组件', () => {
  test('应该正确发送消息', async () => {
    const wrapper = mount(MessageInput, {
      props: {
        chatId: 123,
        chatType: 'private'
      }
    });
    
    // 输入消息内容
    const input = wrapper.find('[data-test="message-input"]');
    await input.setValue('Hello World');
    
    // 点击发送按钮
    const sendBtn = wrapper.find('[data-test="send-btn"]');
    await sendBtn.trigger('click');
    
    // 验证事件触发
    expect(wrapper.emitted('send-message')).toBeTruthy();
    expect(wrapper.emitted('send-message')[0][0]).toEqual({
      content: 'Hello World',
      chatId: 123,
      chatType: 'private'
    });
  });
});
```

### 2. 用户交互测试
**测试内容**:
- 表单验证
- 按钮点击响应
- 键盘快捷键
- 拖拽功能

---

## 🤖 自动化测试

### 1. CI/CD集成
**流水线配置**:
```yaml
# .github/workflows/test.yml
name: 自动化测试

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:13
        env:
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      
      redis:
        image: redis:6.2
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
    - uses: actions/checkout@v3
    
    - name: 设置JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
    
    - name: 缓存Maven依赖
      uses: actions/cache@v3
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
    
    - name: 运行单元测试
      run: mvn test
    
    - name: 运行集成测试
      run: mvn verify -P integration-test
    
    - name: 生成测试报告
      run: mvn jacoco:report
    
    - name: 上传覆盖率报告
      uses: codecov/codecov-action@v3
      with:
        file: ./target/site/jacoco/jacoco.xml
```

### 2. 测试数据管理
**测试数据策略**:
- 使用TestContainers创建隔离环境
- 每个测试用例独立的数据准备
- 测试完成后自动清理数据

---

## 📊 测试报告

### 1. 测试覆盖率报告
**工具**: JaCoCo, SonarQube

**覆盖率要求**:
- 行覆盖率: ≥80%
- 分支覆盖率: ≥70%
- 方法覆盖率: ≥85%

### 2. 性能测试报告
**报告内容**:
- 响应时间分布图
- 吞吐量趋势图
- 错误率统计
- 资源使用情况

### 3. 缺陷统计报告
**统计维度**:
- 缺陷数量趋势
- 缺陷严重程度分布
- 缺陷修复周期
- 缺陷根因分析

---

## 🎯 测试用例设计

### 1. 用户管理模块测试用例

#### TC001: 用户注册功能测试
| 项目 | 内容 |
|------|------|
| **用例编号** | TC001 |
| **用例标题** | 用户注册功能测试 |
| **测试目标** | 验证用户注册功能的正确性 |
| **前置条件** | 系统正常运行，数据库连接正常 |
| **测试步骤** | 1. 访问注册页面<br>2. 输入有效的用户信息<br>3. 点击注册按钮 |
| **预期结果** | 注册成功，返回用户信息 |
| **测试数据** | username: "testuser"<br>password: "Test123456"<br>email: "test@example.com" |

#### TC002: 用户登录功能测试
| 项目 | 内容 |
|------|------|
| **用例编号** | TC002 |
| **用例标题** | 用户登录功能测试 |
| **测试目标** | 验证用户登录功能的正确性 |
| **前置条件** | 用户已注册 |
| **测试步骤** | 1. 访问登录页面<br>2. 输入正确的用户名密码<br>3. 点击登录按钮 |
| **预期结果** | 登录成功，跳转到主页面 |

### 2. 消息功能模块测试用例

#### TC101: 发送文本消息测试
| 项目 | 内容 |
|------|------|
| **用例编号** | TC101 |
| **用例标题** | 发送文本消息测试 |
| **测试目标** | 验证文本消息发送功能 |
| **前置条件** | 用户已登录，存在聊天对象 |
| **测试步骤** | 1. 选择聊天对象<br>2. 输入文本消息<br>3. 点击发送按钮 |
| **预期结果** | 消息发送成功，对方能收到消息 |

---

## 🔍 测试环境管理

### 1. 测试环境配置
**环境分类**:
- **开发测试环境**: 开发人员自测
- **集成测试环境**: 集成测试专用
- **性能测试环境**: 性能测试专用
- **预生产环境**: 生产前最后验证

### 2. 测试数据管理
**数据准备策略**:
- 自动化测试数据生成
- 测试数据版本控制
- 敏感数据脱敏处理
- 测试完成后数据清理

### 3. 环境监控
**监控内容**:
- 服务可用性监控
- 数据库连接监控
- 第三方服务监控
- 测试环境资源使用

---

## 📋 测试检查清单

### 功能测试检查清单
- [ ] 用户注册登录功能
- [ ] 消息发送接收功能
- [ ] 文件上传下载功能
- [ ] 群组管理功能
- [ ] 搜索功能
- [ ] 通知推送功能
- [ ] 用户设置功能

### 性能测试检查清单
- [ ] 负载测试执行完成
- [ ] 压力测试执行完成
- [ ] 稳定性测试执行完成
- [ ] 性能指标达标验证
- [ ] 性能瓶颈分析完成

### 安全测试检查清单
- [ ] 认证授权测试
- [ ] 输入验证测试
- [ ] 数据安全测试
- [ ] 会话管理测试
- [ ] 安全漏洞扫描

### 兼容性测试检查清单
- [ ] 主流浏览器兼容性测试
- [ ] 移动端兼容性测试
- [ ] 不同网络环境测试
- [ ] 不同分辨率适配测试

---

**文档版本**: v1.0.0  
**创建日期**: 2025-11-12  
**维护人**: QA测试团队
