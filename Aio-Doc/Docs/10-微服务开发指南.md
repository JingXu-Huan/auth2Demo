# 微服务开发指南

## 📋 指南功能说明

本开发指南为IM系统的微服务架构提供全面的开发、部署和运维指导。涵盖架构设计、技术选型、开发规范、最佳实践、部署流程、监控告警等内容，帮助开发团队快速理解系统架构，规范开发流程，提升开发效率和代码质量。

### 核心内容

#### 1. 架构设计
- **微服务架构**: 服务拆分原则、服务边界划分
- **分层架构**: Controller、Service、Repository分层设计
- **领域模型**: DDD领域驱动设计实践
- **服务通信**: RESTful API、RPC、消息队列
- **数据一致性**: 分布式事务、最终一致性

#### 2. 技术栈
- **开发框架**: Spring Boot 2.7.x、Spring Cloud Alibaba
- **服务治理**: Nacos（注册中心、配置中心）
- **API网关**: Spring Cloud Gateway
- **负载均衡**: Ribbon、LoadBalancer
- **熔断降级**: Sentinel
- **链路追踪**: Zipkin、Sleuth
- **消息队列**: RabbitMQ
- **缓存**: Redis
- **数据库**: PostgreSQL、MongoDB
- **搜索引擎**: Elasticsearch

#### 3. 开发规范
- **代码规范**: 阿里巴巴Java开发手册
- **命名规范**: 包名、类名、方法名、变量名
- **注释规范**: 类注释、方法注释、参数说明
- **异常处理**: 统一异常处理、自定义异常
- **日志规范**: 日志级别、日志格式、敏感信息脱敏
- **接口规范**: RESTful设计、统一响应格式

#### 4. 最佳实践
- **服务拆分**: 单一职责、高内聚低耦合
- **配置管理**: 配置中心、环境隔离
- **安全设计**: 认证授权、数据加密、防护措施
- **性能优化**: 缓存策略、数据库优化、异步处理
- **容错设计**: 超时控制、重试机制、降级策略
- **测试策略**: 单元测试、集成测试、压力测试

#### 5. 部署运维
- **容器化**: Docker镜像构建、容器编排
- **持续集成**: Jenkins、GitLab CI/CD
- **环境管理**: 开发、测试、预发布、生产环境
- **灰度发布**: 金丝雀发布、蓝绿部署
- **监控告警**: Prometheus、Grafana、告警规则
- **日志收集**: ELK（Elasticsearch、Logstash、Kibana）

#### 6. 问题排查
- **日志分析**: 日志查询、问题定位
- **性能分析**: JVM调优、SQL优化
- **故障处理**: 常见问题、解决方案
- **应急预案**: 故障应急流程、回滚方案

### 使用对象
- **后端开发**: 微服务开发、接口设计
- **运维工程师**: 服务部署、监控运维
- **测试工程师**: 接口测试、性能测试
- **架构师**: 架构设计、技术选型

---

## 📋 目录

1. [架构概览](#架构概览)
2. [技术栈](#技术栈)
3. [服务列表](#服务列表)
4. [开发规范](#开发规范)
5. [部署指南](#部署指南)
6. [监控运维](#监控运维)

---

## 架构概览

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                      客户端层                            │
│  Web浏览器  │  iOS App  │  Android App  │  PC客户端    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│                   API网关层                              │
│              Nginx / Kong / IMGateway                    │
│  - 路由转发  - 认证鉴权  - 限流熔断  - 负载均衡         │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ↓            ↓            ↓
┌──────────┐  ┌──────────┐  ┌──────────┐
│ 用户服务  │  │ 消息服务  │  │ 群组服务  │
│  :8001   │  │  :8002   │  │  :8003   │
└──────────┘  └──────────┘  └──────────┘
        │            │            │
        ↓            ↓            ↓
┌─────────────────────────────────────────┐
│            基础设施层                    │
│  Nacos  │  Redis  │  MySQL  │  Kafka   │
└─────────────────────────────────────────┘
```

### 服务间通信

- **同步调用**: OpenFeign (HTTP/REST)
- **异步消息**: Kafka / RabbitMQ
- **实时通信**: WebSocket / STOMP
- **服务发现**: Nacos
- **配置中心**: Nacos Config

---

## 技术栈

### 后端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.5.7 | 微服务框架 |
| Spring Cloud Gateway | 4.3.0 | API网关 |
| Spring Cloud Alibaba | 2023.0.3.2 | 微服务生态 |
| Nacos | 2.4.2 | 服务注册/配置中心 |
| Redis | 7.0+ | 缓存/消息队列 |
| MySQL | 8.0+ | 关系型数据库 |
| MongoDB | 6.0+ | 文档数据库 |
| Kafka | 3.0+ | 消息队列 |
| Elasticsearch | 8.0+ | 搜索引擎 |

### 前端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4.0 | 前端框架 |
| Vite | 5.0.0 | 构建工具 |
| SockJS | 1.6.1 | WebSocket客户端 |
| STOMP | 7.0.0 | 消息协议 |

---

## 服务列表

### 1. 用户服务 (user-service)

**端口**: 8001  
**职责**: 用户注册、登录、信息管理、好友关系

**核心接口**:
- `POST /api/v1/users/register` - 用户注册
- `POST /api/v1/users/login` - 用户登录
- `GET /api/v1/users/{userId}` - 获取用户信息
- `GET /api/v1/users/friends` - 获取好友列表

**数据库**: MySQL  
**缓存**: Redis (用户信息、Token)

---

### 2. 消息服务 (message-service)

**端口**: 8002  
**职责**: 消息收发、历史记录、已读未读

**核心接口**:
- `POST /api/v1/messages/private` - 发送单聊消息
- `POST /api/v1/messages/group` - 发送群聊消息
- `GET /api/v1/messages/history` - 获取历史消息
- `POST /api/v1/messages/read` - 标记已读

**数据库**: MongoDB (消息存储)  
**缓存**: Redis (未读计数)  
**消息队列**: Kafka (消息分发)

---

### 3. 群组服务 (group-service)

**端口**: 8003  
**职责**: 群组管理、成员管理、权限控制

**核心接口**:
- `POST /api/v1/groups` - 创建群组
- `POST /api/v1/groups/{groupId}/members` - 添加成员
- `GET /api/v1/groups/{groupId}` - 获取群组信息
- `PUT /api/v1/groups/{groupId}/settings` - 群组设置

**数据库**: MySQL  
**缓存**: Redis (群组信息、成员列表)

---

### 4. 在线状态服务 (presence-service)

**端口**: 8004  
**职责**: 用户在线状态、心跳检测、多端登录

**核心接口**:
- `POST /api/v1/presence/online` - 上线
- `POST /api/v1/presence/offline` - 下线
- `GET /api/v1/presence/status/{userId}` - 查询状态
- `POST /api/v1/presence/heartbeat` - 心跳

**数据库**: Redis (状态存储)  
**实时通信**: WebSocket

---

### 5. 文件服务 (file-service)

**端口**: 8005  
**职责**: 文件上传、下载、图片处理

**核心接口**:
- `POST /api/v1/files/upload` - 上传文件
- `GET /api/v1/files/{fileId}` - 下载文件
- `POST /api/v1/files/image/compress` - 图片压缩

**存储**: OSS / MinIO  
**数据库**: MySQL (文件元数据)

---

### 6. 推送服务 (push-service)

**端口**: 8006  
**职责**: 离线消息推送、系统通知

**核心接口**:
- `POST /api/v1/push/send` - 发送推送
- `GET /api/v1/push/history` - 推送历史

**推送渠道**: 
- iOS: APNs
- Android: FCM / 极光推送
- Web: WebSocket / Server-Sent Events

---

### 7. 搜索服务 (search-service)

**端口**: 8007  
**职责**: 全文搜索、消息检索

**核心接口**:
- `GET /api/v1/search/messages` - 搜索消息
- `GET /api/v1/search/users` - 搜索用户
- `GET /api/v1/search/groups` - 搜索群组

**搜索引擎**: Elasticsearch

---

## 开发规范

### 1. 代码规范

#### 包结构
```
com.example.service
├── controller      # 控制器层
├── service         # 业务逻辑层
├── repository      # 数据访问层
├── model           # 数据模型
│   ├── entity      # 实体类
│   ├── dto         # 数据传输对象
│   └── vo          # 视图对象
├── config          # 配置类
├── exception       # 异常处理
└── util            # 工具类
```

#### 命名规范

**Controller**:
```java
@RestController
@RequestMapping("/api/v1/users")
public class UserController {
    
    @PostMapping("/register")
    public Result<UserVO> register(@RequestBody @Valid RegisterRequest request) {
        // ...
    }
}
```

**Service**:
```java
@Service
public class UserService {
    
    public UserVO register(RegisterRequest request) {
        // 业务逻辑
    }
}
```

**Repository**:
```java
public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByUsername(String username);
}
```

### 2. 统一响应格式

```java
@Data
public class Result<T> {
    private Integer code;      // 状态码
    private String message;    // 消息
    private T data;            // 数据
    private Long timestamp;    // 时间戳
    
    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMessage("success");
        result.setData(data);
        result.setTimestamp(System.currentTimeMillis());
        return result;
    }
    
    public static <T> Result<T> error(Integer code, String message) {
        Result<T> result = new Result<>();
        result.setCode(code);
        result.setMessage(message);
        result.setTimestamp(System.currentTimeMillis());
        return result;
    }
}
```

### 3. 异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public Result<?> handleBusinessException(BusinessException e) {
        return Result.error(e.getCode(), e.getMessage());
    }
    
    @ExceptionHandler(Exception.class)
    public Result<?> handleException(Exception e) {
        log.error("系统异常", e);
        return Result.error(500, "系统内部错误");
    }
}
```

### 4. 日志规范

```java
@Slf4j
@Service
public class UserService {
    
    public UserVO register(RegisterRequest request) {
        log.info("用户注册开始, username={}", request.getUsername());
        
        try {
            // 业务逻辑
            log.info("用户注册成功, userId={}", user.getId());
            return userVO;
        } catch (Exception e) {
            log.error("用户注册失败, username={}", request.getUsername(), e);
            throw new BusinessException("注册失败");
        }
    }
}
```

### 5. 配置管理

**application.yml**:
```yaml
spring:
  application:
    name: user-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:localhost:8848}
      config:
        server-addr: ${NACOS_SERVER:localhost:8848}
        file-extension: yaml
```

---

## 部署指南

### 1. 本地开发环境

#### 启动基础设施

```bash
# 启动Redis
redis-server

# 启动MySQL
mysql.server start

# 启动Nacos
sh startup.sh -m standalone
```

#### 启动微服务

```bash
# 用户服务
cd user-service
mvn spring-boot:run

# 消息服务
cd message-service
mvn spring-boot:run

# IM网关
cd IMgateway
mvn spring-boot:run
```

### 2. Docker部署

**docker-compose.yml**:
```yaml
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root123
    ports:
      - "3306:3306"
  
  nacos:
    image: nacos/nacos-server:v2.4.2
    environment:
      MODE: standalone
    ports:
      - "8848:8848"
  
  user-service:
    build: ./user-service
    ports:
      - "8001:8001"
    depends_on:
      - redis
      - mysql
      - nacos
```

**启动**:
```bash
docker-compose up -d
```

### 3. Kubernetes部署

**deployment.yaml**:
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: user-service:1.0.0
        ports:
        - containerPort: 8001
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
```

---

## 监控运维

### 1. 健康检查

所有服务都集成了Spring Boot Actuator:

```bash
# 健康检查
curl http://localhost:8001/actuator/health

# 服务信息
curl http://localhost:8001/actuator/info

# 指标数据
curl http://localhost:8001/actuator/metrics
```

### 2. 日志收集

使用ELK Stack:
- **Elasticsearch**: 日志存储
- **Logstash**: 日志收集
- **Kibana**: 日志可视化

### 3. 监控告警

使用Prometheus + Grafana:

```yaml
# prometheus.yml
scrape_configs:
  - job_name: 'spring-boot'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['localhost:8001', 'localhost:8002']
```

### 4. 链路追踪

使用Spring Cloud Sleuth + Zipkin:

```yaml
spring:
  sleuth:
    sampler:
      probability: 1.0
  zipkin:
    base-url: http://localhost:9411
```

---

## 常见问题

### Q1: 服务启动失败？

**检查清单**:
1. 端口是否被占用
2. Nacos是否启动
3. Redis/MySQL是否可连接
4. 配置文件是否正确

### Q2: 服务间调用失败？

**排查步骤**:
1. 检查服务是否注册到Nacos
2. 检查网络连通性
3. 查看日志错误信息
4. 验证Token是否有效

### Q3: 消息发送失败？

**可能原因**:
1. WebSocket连接断开
2. Redis连接失败
3. 接收者不在线
4. 消息格式错误

---

## 快速开始

### 1. 克隆项目
```bash
git clone https://github.com/your-repo/im-system.git
cd im-system
```

### 2. 安装依赖
```bash
mvn clean install
```

### 3. 启动服务
```bash
# 启动基础设施
docker-compose up -d redis mysql nacos

# 启动微服务
./start-all.sh
```

### 4. 访问前端
```bash
cd frontend
npm install
npm run dev
```

访问：http://localhost:3000

---

**文档版本**: v1.0.0  
**最后更新**: 2025-11-09  
**维护团队**: 开发团队
